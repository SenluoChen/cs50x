// NOTE: These UI size settings are intentionally centralized and locked.
// Do NOT edit these values directly — change policy requires explicit review.
export const MEDIA_HEIGHTS = Object.freeze({ xs: 320, sm: 420, md: 480 });

// Grid columns for the media row: poster : trailer (approx 1:3 on md+)
export const MEDIA_GRID_COLUMNS = Object.freeze({ xs: "1fr", md: "1fr 3fr" });

export const DETAILS_GRID_COLUMNS = Object.freeze({ xs: "1fr", md: "1fr 360px" });
